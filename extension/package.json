{
  "name": "pelangganpro-extension",
  "version": "1.0.0",
  "description": "PelangganPro CRM Extension for WhatsApp Web",
  "scripts": {
    "build": "node build.mjs",
    "build:watch": "node build.mjs --watch",
    "dev": "node build.mjs --dev"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "preact": "^10.19.0",
    "zustand": "^4.5.0"
  }
}
